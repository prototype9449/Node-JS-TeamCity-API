#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jsondiffpatch/bin/jsondiffpatch" "$@"
  ret=$?
else 
  node  "$basedir/../jsondiffpatch/bin/jsondiffpatch" "$@"
  ret=$?
fi
exit $ret
