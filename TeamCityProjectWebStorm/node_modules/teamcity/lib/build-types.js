(function() {
  module.exports = function(client) {
    return function(locator, cb) {
      var id;
      if (typeof locator === 'function') {
        cb = locator;
        return client._get('/app/rest/buildTypes', cb);
      } else if (locator.compile) {
        return client._get('/app/rest/buildTypes', {
          locator: locator.compile()
        }, cb);
      } else {
        id = locator;
        return client._get("/app/rest/buildTypes/" + id, cb);
      }
    };
  };

}).call(this);
